/**
 * @file Shared labels and defaults for layout/slide chrome editors
 *
 * Used by `SlideLayoutChromeEditors` and PPTX/POTX hosts so section titles stay aligned.
 */

/** Title for slide/layout color map override editor (p:clrMap override). */
export const LAYOUT_CHROME_COLOR_MAP_OVERRIDE_TITLE = "Color Map Override";

/** `OptionalPropertySection` title wrapping `TransitionEditor`. */
export const LAYOUT_CHROME_TRANSITION_SECTION_TITLE = "Transition";

/** Default collapsed state for the transition section in inspectors. */
export const LAYOUT_CHROME_TRANSITION_DEFAULT_EXPANDED = false;
