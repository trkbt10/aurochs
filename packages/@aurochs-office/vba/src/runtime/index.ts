/**
 * @file VBA Runtime exports
 *
 * VBA core runtime for executing parsed VBA programs.
 * This is a minimal subset implementation (Phase 3).
 */

// Runtime types will be added in Phase 3
// For now, export nothing to satisfy the module
export {};
